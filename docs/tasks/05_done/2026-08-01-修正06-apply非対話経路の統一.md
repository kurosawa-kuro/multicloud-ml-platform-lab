# 修正06: apply の非対話経路を5基盤で統一する

Weight Class: Light（docstring と runbook の統一。コード変更は最小）
親調査: [2026-08-01-5基盤完走後の再設計と修正順序.md](./2026-08-01-5基盤完走後の再設計と修正順序.md) §2.8 / 順序#6

## Goal

「保存済み plan を渡して apply する」手順（レビュー内容 = 適用内容・承認プロンプトなし）を
5 runbook と `scripts/run_terraform.py` の docstring に統一して書く。
TTY 無しで素の `make tf-apply` を叩いて `EOF` 失敗 + 偽 failure 行が残る事故の再発を防ぐ。

## Value

collection accuracy / docs canonicalization。2026-08-01 に実際に偽 failure 行が
`infra_events` に入り、`04_azureml.md` に除外注記を書く羽目になった。

## Context

- `run_terraform.py` は `parse_known_args` で plan ファイル渡しに**対応済み**。
  周知されていないだけ（Azure / Databricks の runbook にしか書かれていない）
- `-auto-approve` を付けない設計（plan-first）は**維持する**

## Scope

- `run_terraform.py` docstring に標準手順を明記
- 5 runbook の apply 節を同じ形に（`plan -out=` → `run_terraform.py apply <env> <planfile>`）
- 可能なら: TTY が無い状態で planfile なしに呼ばれたら **記録前に** EXIT_USAGE で落とす
  ガード（偽 failure 行を作らない）

## Non-scope

- `-auto-approve` の既定化（しない。plan-first 原則）
- Makefile の tf-apply の挙動変更（planfile 引数の追加は可）

## Plan

1. TTY 検出ガードのテスト（`tests/test_run_terraform.py`）
2. docstring + runbook 5本更新
3. `make test` green

## Acceptance Criteria

- [ ] 5 runbook の apply 手順が同一の形
- [ ] TTY 無し + planfile 無しの呼び出しが `infra_events` に行を残さず EXIT_USAGE
- [ ] `make test` green

## Stop / Ask Owner If

- （想定なし。ガード実装が run_terraform の記録契約と衝突したら報告）

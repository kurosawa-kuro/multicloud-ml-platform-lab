# 修正01: parity テストの skip 解除（安全網の確立）

Weight Class: Standard（テストのみ・実クラウド不要）
親調査: [2026-08-01-5基盤完走後の再設計と修正順序.md](./2026-08-01-5基盤完走後の再設計と修正順序.md) §3.2 / G4 / 順序#1

## Goal

`tests/test_metric_parity.py` / `tests/test_code_revision_parity.py` の
`@pytest.mark.skip(reason="Phase 0 未着手")` 5件を外し、5基盤の実測値
（RMSE `0.4368055090296257`）を期待値として `make test` の既定ゲートにする。

## Value

failure detection。5基盤一致という主成果が現在テストで守られておらず、
`core/ml` を壊しても緑のまま。以降の全修正の安全網になるため最優先。

## Context

skip 理由「Phase 0 未着手」は陳腐化（Phase 0〜5 すべて完了）。
`sql/comparison_queries.sql` クエリ1 のコメントは「test_metric_parity.py が
機械判定する対象」と宣言しており、宣言と実態が乖離している。

## Scope

- 2ファイルの skip 解除と期待値の固定
- 揺れの検証: `make test` を5回連続実行（親調査 H-6）

## Non-scope

- 学習ロジック・データ層の変更（揺れが出た場合は原因調査タスクを別途切る）
- Neon への実接続テスト化（既存のテスト構造を維持する）

## Plan

1. skip デコレータを外し、テストが何を期待しているか読む
2. 期待値を実測値で固定（許容誤差は入れない。完全一致が実測事実）
3. `make test` ×5 で揺れないことを確認

## Acceptance Criteria

- [ ] `pytest -rs` に「Phase 0 未着手」の skip が 0 件
- [ ] `make test` 5回連続 all green
- [ ] 期待値がハードコードではなく定数モジュール等の1箇所にある

## Stop / Ask Owner If

- 5回実行で値が揺れた（seed / ライブラリ版の問題。許容誤差でごまかさず停止して報告）

# Refactoring Candidates

`docs/tasks/README.md` から参照される Active タスクドキュメント。日々見る cleanup / refactoring 候補をここに集約する。

このファイルは「いつか整理する」置き場ではなく、毎日の作業開始時に見る運用リストである。候補が実行可能になったら、必要に応じて個別 task file を `03_active/` に作る。

## 候補一覧

| # | 対象 | 内容 | 優先度 |
|---|------|------|--------|
| 1 | 大改修 P0〜P8 | [調査と方針 第2版](../01_idea/2026-08-02-大改修-5インフラ制約起点の再設計-調査と方針.md)を `02_backlog/` の9タスク（`2026-08-02-大改修P0`〜`P8`）に分割済み。**検証戦略は owner 採用済み**（P0〜P7 ローカル証拠 → P8 キャンペーン1回 → 最終修正・コミットは P 単位）。残る owner 判断は D1 / P3 移設先名。P0 は判断と独立に着手可 | 高 |

## 運用

- 新しい候補は上表に 1 行追加し、必要なら本文に詳細節を足す。
- すぐ着手する候補は、対象・目的・完了条件を明確にしてから実行する。
- 未着手の候補は `docs/tasks/02_backlog/`、完了した判断ノートは `docs/tasks/05_done/` へ移す。
- 確定した仕様は `docs/specs/`、判断理由は `docs/decisions/` に昇格する。
- 運用手順になったものは `docs/runbooks/`、繰り返し使う作業手順は `.claude/skills/` に分離する。
- ドキュメント構成のルールは [../../00_index.md](../../00_index.md) を参照。

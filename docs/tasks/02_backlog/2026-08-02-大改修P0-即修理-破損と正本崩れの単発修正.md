# 大改修 P0: 即修理 — 破損と正本崩れの単発修正

Weight Class: Standard / 親調査: [大改修 調査と方針 第2版](../01_idea/2026-08-02-大改修-5インフラ制約起点の再設計-調査と方針.md) §5-P0
**大改修の方針採否と独立に着手できる**（全件が現行バグ・drift の修理）。

## Goal

現に壊れている・嘘になっている6件を単発修正で潰す。

## Value

integrity / dev speed（`make dbx-upload` の復旧と、静かに死んでいる設定の除去）。

## Context

第2回調査で実物確認済み（file:line は親調査 §4 参照）。いずれも導出順序と無関係に「今すでに壊れている」。

## Scope

1. `Makefile:154` 旧パス import 修正（`platforms.config` → `platforms.shared.config`）— `make dbx-upload` 復旧
2. `Makefile:157` wheel ファイル名直書き → `packaging_names.wheel_filename()` 経由
3. `env/config.yaml:63` `experiment_name` → `experiment`（無言 drop の実害）
4. `docker/orchestrator/Dockerfile:56` `pyyaml>=6.0,<7.0` → `<6.1`（同一 minor ルール違反）
5. `Makefile` の `tf-destroy` に `check-residual --record` を付与（Golden Path④ 一次データの標準経路復旧）
6. docstring / パス drift の一括修正: `vertex/adapter.py:330`（TEARDOWN コメント矛盾）/ `training/Dockerfile:40`（「未配線」）/ `resume.py:23`（消滅関数参照）/ `02_architecture.md:72-73`・`05_data_model.md:75`・`env/config.yaml:12,75,89` の旧パス / config.yaml 死にキー（`appEnv` `port` `logLevel` `neon.*`・Azure region コメント）

## Non-scope

- 番人テストの新設・修理（→ P1）
- 依存 pin の契約テスト拡張（→ P4）。ここでは pyyaml の値修正のみ
- Makefile のガード節・`tf-init` の修理（→ P5）

## Done

6件すべて修正され、`make dbx-upload` が ImportError なく動く（wheel ビルドまで。クラウド接続は不要）。

## Evidence

Level 2: `make lint` / `make test` 緑 + 修正1は `make dbx-upload` の import 部分が通ることをコマンド出力で（doppler/実接続は不要な範囲で確認）。修正5は `make tf-destroy` のレシピに `--record` が入った diff と、`check_residual --record` の単体実行が record 経路に入ることの既存テスト緑。

## Stop / Ask Owner If

- 修正中に scope 外の同種 drift を見つけたら実装せず本 task の Notes に追記（control-change）
- config.yaml の死にキー削除で「実は使う予定だった」ものが疑われたら削除せず確認
